# Orchestration Log — K (Frontend Dev)

**Session:** 2026-06-17T15:02:21+02:00  
**Agent:** K (Frontend Dev, Claude Sonnet 4.6, background)

## Summary

**Fixed 3 critical UI issues in solutions UI and topology rendering:**

1. **Cribl sizing drawer for Trend Micro CEF solutions** — Added explicit `capacity_type: "eps"` to syslog/CEF solutions (Trend Micro Deep Security, Tipping Point, Apex One) that lack firewall-family naming heuristics. Topology now correctly routes them through Cribl when selected, even without drawer interaction.

2. **Topology Cribl routing explicit opt-in** — Modified `isCriblRoutedSolution` to require saved `criblIngestionExplicit: true` for Cribl routing to activate. Fallback behavior now respects user intent: no saved preference = no routing, avoiding state-presence asymmetry between Solutions UI and topology.

3. **Task Name column width in Gantt planner** — Widened column in both CSS fallback and `gantt-planner.js` default metadata because runtime JS overwrites static CSS via `--gantt-table-columns` variable.

## Files Modified

- `data/solutions.json` — Added `capacity_type: "eps"` to 3 solutions
- `js/modules/topology.js` — Updated `isCriblRoutedSolution` routing logic
- `js/gantt-planner.js` — Column width defaults
- `css/style.css` — Task Name column width fallback

## Status

✅ Completed. Decisions recorded in decisions.md.
